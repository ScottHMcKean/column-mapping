databricks:
  catalog: shm
  schema: columnmapping
  table_prefix: silver
  warehouse_id: 148ccb90800933a1

tables:
  rules_table_name: standardization_rules
  mappings_table_name: standardization_mappings
  canonical_columns_table_name: canonical_columns

vector_search:
  endpoint_name: dbdemos_vs_endpoint
  index_name: standardization_mappings_index
  embedding_model_endpoint: databricks-bge-large-en
  delta_sync:
    primary_key: mapping_id
    embedding_source_column: platform_header
    pipeline_type: TRIGGERED

llm:
  endpoint: databricks-claude-sonnet-4-5

mapping:
  top_k: 5
