resources:
  apps:
    column_mapper:
      name: ${var.app_name}
      description: "Column Mapper -- standardize and match column headers across platforms"
      source_code_path: ..
      resources:
        - name: sql-warehouse
          sql_warehouse:
            id: ${var.warehouse_id}
            permission: CAN_USE
